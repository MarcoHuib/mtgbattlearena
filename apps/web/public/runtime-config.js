// Local/offline default. Release deployments replace this file after downloading
// the immutable frontend artifact and before deploying the selected environment.
window.__MTG_RUNTIME_CONFIG__ = Object.freeze({})
