# Publieke Beta-endpoints. Vite laadt dit bestand via `vite build --mode staging`.
VITE_IMPORT_API_URL=https://api.beta.mtgbattlearena.nl
VITE_ONLINE_API_URL=https://api.beta.mtgbattlearena.nl
VITE_ONLINE_SOCKET_URL=https://ws.beta.mtgbattlearena.nl

# Beta en Production gebruiken bewust dezelfde Firebase Authentication-webconfig.
# Die publieke config wordt tijdens builds via gelijknamige GitHub Environment
# variables geleverd. Commit hier nooit private keys of serviceaccounts.
