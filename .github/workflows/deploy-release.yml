name: Deploy Release
run-name: "Release #${{ github.run_number }} · ${{ github.sha }}"

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

env:
  RELEASE_VERSION: ${{ github.run_number }}

concurrency:
  group: release-main
  cancel-in-progress: false

jobs:
  changes:
    name: Release / Detect changes
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      frontend: ${{ steps.filter.outputs.frontend }}
      game_worker: ${{ steps.filter.outputs.game_worker }}
      import_worker: ${{ steps.filter.outputs.import_worker }}
      any: ${{ steps.filter.outputs.any }}
    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Determine affected deployables
        id: filter
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail

          frontend=false
          game_worker=false
          import_worker=false

          if [[ "$EVENT_NAME" == "workflow_dispatch" || -z "$BASE_SHA" || "$BASE_SHA" =~ ^0+$ ]]; then
            frontend=true
            game_worker=true
            import_worker=true
          else
            while IFS= read -r -d '' file; do
              case "$file" in
                package.json|package-lock.json|.github/workflows/deploy-release.yml)
                  frontend=true
                  game_worker=true
                  import_worker=true
                  ;;
                tsconfig.json|tsconfig.base.json|tsconfig.tools.json|packages/*)
                  frontend=true
                  game_worker=true
                  ;;
                apps/web/*|scripts/write-runtime-config.mjs|firebase.json|.firebaserc)
                  frontend=true
                  ;;
                apps/game-worker/*)
                  game_worker=true
                  ;;
                apps/import-worker/*)
                  import_worker=true
                  ;;
              esac
            done < <(git diff --name-only -z "$BASE_SHA" "$HEAD_SHA")
          fi

          any=false
          if [[ "$frontend" == true || "$game_worker" == true || "$import_worker" == true ]]; then
            any=true
          fi

          echo "frontend=$frontend" >> "$GITHUB_OUTPUT"
          echo "game_worker=$game_worker" >> "$GITHUB_OUTPUT"
          echo "import_worker=$import_worker" >> "$GITHUB_OUTPUT"
          echo "any=$any" >> "$GITHUB_OUTPUT"

  build-frontend:
    name: Build / Frontend
    needs: changes
    if: needs.changes.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Frontend unchanged
        if: needs.changes.outputs.frontend != 'true'
        run: echo "Frontend is unchanged; build is not required."
      - name: Check out repository
        if: needs.changes.outputs.frontend == 'true'
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Set up Node.js
        if: needs.changes.outputs.frontend == 'true'
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        if: needs.changes.outputs.frontend == 'true'
        run: npm ci
      - name: Lint frontend and shared packages
        if: needs.changes.outputs.frontend == 'true'
        run: npm run lint
      - name: Type-check frontend and shared packages
        if: needs.changes.outputs.frontend == 'true'
        run: |
          npx --no-install tsc -p packages/game-core/tsconfig.json --noEmit
          npx --no-install tsc -p packages/game-protocol/tsconfig.json --noEmit
          npx --no-install tsc -p apps/web/tsconfig.app.json --noEmit
          npx --no-install tsc -p apps/web/tsconfig.node.json --noEmit
      - name: Test shared packages
        if: needs.changes.outputs.frontend == 'true'
        run: npm run test:packages
      - name: Test frontend
        if: needs.changes.outputs.frontend == 'true'
        run: npm run test:web
      - name: Build environment-neutral PWA
        if: needs.changes.outputs.frontend == 'true'
        run: npm run build --workspace @mtg/web
      - name: Upload immutable frontend artifact
        if: needs.changes.outputs.frontend == 'true'
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: frontend-release-${{ github.run_number }}
          path: apps/web/dist
          if-no-files-found: error
          retention-days: 14

  build-game-worker:
    name: Build / Game Worker
    needs: changes
    if: needs.changes.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Game Worker unchanged
        if: needs.changes.outputs.game_worker != 'true'
        run: echo "Game Worker is unchanged; build is not required."
      - name: Check out repository
        if: needs.changes.outputs.game_worker == 'true'
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Set up Node.js
        if: needs.changes.outputs.game_worker == 'true'
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        if: needs.changes.outputs.game_worker == 'true'
        run: npm ci
      - name: Lint Game Worker and shared packages
        if: needs.changes.outputs.game_worker == 'true'
        run: npx --no-install eslint apps/game-worker packages
      - name: Type-check Game Worker
        if: needs.changes.outputs.game_worker == 'true'
        run: npm run worker:type-check
      - name: Test Game Worker
        if: needs.changes.outputs.game_worker == 'true'
        run: npm run test:worker
      - name: Bundle Beta Game Worker
        if: needs.changes.outputs.game_worker == 'true'
        run: npm run deploy:cloudflare:check:staging:game
      - name: Bundle Production Game Worker
        if: needs.changes.outputs.game_worker == 'true'
        run: npm run deploy:cloudflare:check:game

  build-import-worker:
    name: Build / Import Worker
    needs: changes
    if: needs.changes.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Import Worker unchanged
        if: needs.changes.outputs.import_worker != 'true'
        run: echo "Import Worker is unchanged; build is not required."
      - name: Check out repository
        if: needs.changes.outputs.import_worker == 'true'
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Set up Node.js
        if: needs.changes.outputs.import_worker == 'true'
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        if: needs.changes.outputs.import_worker == 'true'
        run: npm ci
      - name: Validate Import Worker
        if: needs.changes.outputs.import_worker == 'true'
        run: npm run validate:import-worker

  build-complete:
    name: Build / Complete
    needs:
      - changes
      - build-frontend
      - build-game-worker
      - build-import-worker
    if: always() && needs.changes.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Require all release builds
        env:
          FRONTEND_RESULT: ${{ needs.build-frontend.result }}
          GAME_RESULT: ${{ needs.build-game-worker.result }}
          IMPORT_RESULT: ${{ needs.build-import-worker.result }}
        run: |
          set -euo pipefail
          [[ "$FRONTEND_RESULT" == success ]]
          [[ "$GAME_RESULT" == success ]]
          [[ "$IMPORT_RESULT" == success ]]

  release-beta-frontend:
    name: Beta / Frontend
    needs:
      - changes
      - build-complete
    if: >-
      always() &&
      needs.changes.outputs.frontend == 'true' &&
      needs.build-complete.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment: staging
    steps:
      - name: Check out deployment configuration
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Download release artifact
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          name: frontend-release-${{ github.run_number }}
          path: apps/web/dist
      - name: Add Beta runtime configuration
        env:
          # Environment Variables from the active `staging` environment.
          APP_ENV: ${{ vars.APP_ENV }}
          IMPORT_API_URL: ${{ vars.IMPORT_API_URL }}
          ONLINE_API_URL: ${{ vars.ONLINE_API_URL }}
          ONLINE_SOCKET_URL: ${{ vars.ONLINE_SOCKET_URL }}
          RELEASE_VERSION: ${{ github.run_number }}
          # Repository Variables shared by Beta and Production.
          FIREBASE_API_KEY: ${{ vars.FIREBASE_API_KEY }}
          FIREBASE_AUTH_DOMAIN: ${{ vars.FIREBASE_AUTH_DOMAIN }}
          FIREBASE_PROJECT_ID: ${{ vars.FIREBASE_PROJECT_ID }}
          FIREBASE_APP_ID: ${{ vars.FIREBASE_APP_ID }}
          RUNTIME_CONFIG_OUTPUT: apps/web/dist/runtime-config.js
        run: node scripts/write-runtime-config.mjs
      - name: Deploy fixed Beta Hosting site
        uses: FirebaseExtended/action-hosting-deploy@7c850a480ce753f4f06f010801fc5a43787740bb # v0
        with:
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_MTGBATTLEARENA }}
          projectId: mtgbattlearena
          target: staging
          channelId: live
          entryPoint: .
          firebaseToolsVersion: 15.26.0

  release-beta-import-worker:
    name: Beta / Import Worker
    needs:
      - changes
      - build-complete
    if: >-
      needs.changes.outputs.import_worker == 'true' &&
      needs.build-complete.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment: staging
    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Set up Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Deploy Beta import Worker
        uses: cloudflare/wrangler-action@ebbaa1584979971c8614a24965b4405ff95890e0 # v4.0.0
        env:
          RELEASE_VERSION: ${{ github.run_number }}
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          wranglerVersion: 4.120.0
          workingDirectory: apps/import-worker
          command: deploy --env staging --config wrangler.toml
          packageManager: npm
          vars: RELEASE_VERSION

  release-beta-game-worker:
    name: Beta / Game Worker
    needs:
      - changes
      - build-complete
      - release-beta-import-worker
    if: >-
      always() &&
      needs.changes.outputs.game_worker == 'true' &&
      needs.build-complete.result == 'success' &&
      (needs.changes.outputs.import_worker != 'true' ||
      needs.release-beta-import-worker.result == 'success')
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment: staging
    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Set up Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Deploy Beta game Worker
        uses: cloudflare/wrangler-action@ebbaa1584979971c8614a24965b4405ff95890e0 # v4.0.0
        env:
          RELEASE_VERSION: ${{ github.run_number }}
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          wranglerVersion: 4.120.0
          workingDirectory: apps/game-worker
          command: deploy --env staging --config wrangler.toml
          packageManager: npm
          vars: RELEASE_VERSION

  beta-complete:
    name: Beta / Complete
    needs:
      - changes
      - build-complete
      - release-beta-frontend
      - release-beta-import-worker
      - release-beta-game-worker
    if: always() && needs.build-complete.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Require every affected Beta deployment
        shell: bash
        env:
          FRONTEND_REQUIRED: ${{ needs.changes.outputs.frontend }}
          FRONTEND_RESULT: ${{ needs.release-beta-frontend.result }}
          IMPORT_REQUIRED: ${{ needs.changes.outputs.import_worker }}
          IMPORT_RESULT: ${{ needs.release-beta-import-worker.result }}
          GAME_REQUIRED: ${{ needs.changes.outputs.game_worker }}
          GAME_RESULT: ${{ needs.release-beta-game-worker.result }}
        run: |
          set -euo pipefail
          [[ "$FRONTEND_REQUIRED" != true || "$FRONTEND_RESULT" == success ]]
          [[ "$IMPORT_REQUIRED" != true || "$IMPORT_RESULT" == success ]]
          [[ "$GAME_REQUIRED" != true || "$GAME_RESULT" == success ]]

  publish-beta-release:
    name: Beta / Publish release metadata
    needs:
      - changes
      - beta-complete
    if: >-
      needs.changes.outputs.any == 'true' &&
      needs.beta-complete.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      deployments: write
    steps:
      - name: Publish successful Beta release number
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_VERSION: ${{ github.run_number }}
        run: |
          set -euo pipefail
          jq -n \
            --arg ref "$GITHUB_SHA" \
            --arg description "Build #$RELEASE_VERSION" \
            '{ref: $ref, task: "release-metadata", environment: "staging", description: $description, auto_merge: false, required_contexts: [], transient_environment: false, production_environment: false}' \
            | gh api --method POST "repos/$GITHUB_REPOSITORY/deployments" --input - --jq '.id' \
            > deployment-id.txt
          deployment_id="$(<deployment-id.txt)"
          jq -n \
            --arg description "Build #$RELEASE_VERSION" \
            --arg log_url "$GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID" \
            '{state: "success", description: $description, environment_url: "https://beta.mtgbattlearena.nl", log_url: $log_url}' \
            | gh api --method POST "repos/$GITHUB_REPOSITORY/deployments/$deployment_id/statuses" --input -

  release-production-frontend:
    name: Production / Frontend
    needs:
      - changes
      - beta-complete
    if: >-
      always() &&
      needs.changes.outputs.frontend == 'true' &&
      needs.beta-complete.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment: production
    steps:
      - name: Check out deployment configuration
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Download the same release artifact
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          name: frontend-release-${{ github.run_number }}
          path: apps/web/dist
      - name: Add Production runtime configuration
        env:
          # Environment Variables from the active `production` environment.
          APP_ENV: ${{ vars.APP_ENV }}
          IMPORT_API_URL: ${{ vars.IMPORT_API_URL }}
          ONLINE_API_URL: ${{ vars.ONLINE_API_URL }}
          ONLINE_SOCKET_URL: ${{ vars.ONLINE_SOCKET_URL }}
          RELEASE_VERSION: ${{ github.run_number }}
          # Repository Variables shared by Beta and Production.
          FIREBASE_API_KEY: ${{ vars.FIREBASE_API_KEY }}
          FIREBASE_AUTH_DOMAIN: ${{ vars.FIREBASE_AUTH_DOMAIN }}
          FIREBASE_PROJECT_ID: ${{ vars.FIREBASE_PROJECT_ID }}
          FIREBASE_APP_ID: ${{ vars.FIREBASE_APP_ID }}
          RUNTIME_CONFIG_OUTPUT: apps/web/dist/runtime-config.js
        run: node scripts/write-runtime-config.mjs
      - name: Deploy fixed Production Hosting site
        uses: FirebaseExtended/action-hosting-deploy@7c850a480ce753f4f06f010801fc5a43787740bb # v0
        with:
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_MTGBATTLEARENA }}
          projectId: mtgbattlearena
          target: production
          channelId: live
          entryPoint: .
          firebaseToolsVersion: 15.26.0

  release-production-import-worker:
    name: Production / Import Worker
    needs:
      - changes
      - beta-complete
    if: >-
      needs.changes.outputs.import_worker == 'true' &&
      needs.beta-complete.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment: production
    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Set up Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Deploy Production import Worker
        uses: cloudflare/wrangler-action@ebbaa1584979971c8614a24965b4405ff95890e0 # v4.0.0
        env:
          RELEASE_VERSION: ${{ github.run_number }}
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          wranglerVersion: 4.120.0
          workingDirectory: apps/import-worker
          command: deploy --config wrangler.toml
          packageManager: npm
          vars: RELEASE_VERSION

  release-production-game-worker:
    name: Production / Game Worker
    needs:
      - changes
      - beta-complete
      - release-production-import-worker
    if: >-
      always() &&
      needs.changes.outputs.game_worker == 'true' &&
      needs.beta-complete.result == 'success' &&
      (needs.changes.outputs.import_worker != 'true' ||
      needs.release-production-import-worker.result == 'success')
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment: production
    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Set up Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Deploy Production game Worker
        uses: cloudflare/wrangler-action@ebbaa1584979971c8614a24965b4405ff95890e0 # v4.0.0
        env:
          RELEASE_VERSION: ${{ github.run_number }}
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          wranglerVersion: 4.120.0
          workingDirectory: apps/game-worker
          command: deploy --config wrangler.toml
          packageManager: npm
          vars: RELEASE_VERSION

  production-complete:
    name: Production / Complete
    needs:
      - changes
      - beta-complete
      - release-production-frontend
      - release-production-import-worker
      - release-production-game-worker
    if: always() && needs.beta-complete.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Require every affected Production deployment
        shell: bash
        env:
          FRONTEND_REQUIRED: ${{ needs.changes.outputs.frontend }}
          FRONTEND_RESULT: ${{ needs.release-production-frontend.result }}
          IMPORT_REQUIRED: ${{ needs.changes.outputs.import_worker }}
          IMPORT_RESULT: ${{ needs.release-production-import-worker.result }}
          GAME_REQUIRED: ${{ needs.changes.outputs.game_worker }}
          GAME_RESULT: ${{ needs.release-production-game-worker.result }}
        run: |
          set -euo pipefail
          [[ "$FRONTEND_REQUIRED" != true || "$FRONTEND_RESULT" == success ]]
          [[ "$IMPORT_REQUIRED" != true || "$IMPORT_RESULT" == success ]]
          [[ "$GAME_REQUIRED" != true || "$GAME_RESULT" == success ]]

  publish-production-release:
    name: Production / Publish release metadata
    needs:
      - changes
      - production-complete
    if: >-
      needs.changes.outputs.any == 'true' &&
      needs.production-complete.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      deployments: write
    steps:
      - name: Publish successful Production release number
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_VERSION: ${{ github.run_number }}
        run: |
          set -euo pipefail
          jq -n \
            --arg ref "$GITHUB_SHA" \
            --arg description "Build #$RELEASE_VERSION" \
            '{ref: $ref, task: "release-metadata", environment: "production", description: $description, auto_merge: false, required_contexts: [], transient_environment: false, production_environment: true}' \
            | gh api --method POST "repos/$GITHUB_REPOSITORY/deployments" --input - --jq '.id' \
            > deployment-id.txt
          deployment_id="$(<deployment-id.txt)"
          jq -n \
            --arg description "Build #$RELEASE_VERSION" \
            --arg log_url "$GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID" \
            '{state: "success", description: $description, environment_url: "https://mtgbattlearena.nl", log_url: $log_url}' \
            | gh api --method POST "repos/$GITHUB_REPOSITORY/deployments/$deployment_id/statuses" --input -
